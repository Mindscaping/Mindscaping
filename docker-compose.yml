services:
  app:
    build: .
    ports:
      - "3004:3000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=file:/app/data/prod.db
    volumes:
      - db-data:/app/data

volumes:
  db-data:
